# NS-AIME アルゴリズム整理

対象論文：T. Nakanishi, **"NS-AIME: Rule-Guided Re-Constraint of Approximate Inverse Explanations"**, 4th World Conference on eXplainable AI (Late-breaking, Demos and Doctoral Consortium), Fortaleza, Brazil, 2026.

このドキュメントは「論文のアルゴリズムの整理」「論文が AIME / SHAP / LIME / ルール学習器と **どこが違うか**」「**理論と実装の対応関係**（および本リポジトリの既存実装からの修正点）」をまとめたものである。曖昧な箇所は **仮定** として明示する。

---

## 1. 位置づけ（一言で）

NS-AIME は新しい予測器でも新しいルール学習器でもない。**学習済みブラックボックス** `f` を AIME の逆作用素で説明し、その**大域的属性行列の「形」だけを、データから誘導した論理ルールに近づくよう凸最適化で再制約する post-hoc 層**である。

> AIME（逆作用素ベース XAI）が出す「数学的に well-defined だが単調性・閾値のような人間が期待する論理構造を持たない説明」と、「人間が監査しやすいルール的説明」の**中間層**を、ブラックボックスを再学習せずに差し込む。

---

## 2. 記号

| 記号 | 意味 |
|---|---|
| `X ∈ R^{n×d}` | データ行列（`x ∈ R^d`、表形式 or TF–IDF などのベクトル化入力） |
| `Ŷ = f(X) ∈ R^{n×m}` | 学習済み分類器の**予測クラス確率** |
| `A† ∈ R^{d×m}` | AIME の逆作用素（大域属性行列）。第 `k` 列がクラス `k` の大域寄与ベクトル |
| `M_{jk} ∈ {−1,0,+1}` | 特徴 `j`・クラス `k` の単調性符号 |
| `t_{jk}` | 特徴 `j`・クラス `k` の閾値 |
| `B ∈ R^{d×m}` | 再制約後の説明行列（NS-AIME の出力） |

---

## 3. アルゴリズム（4 ステップ）

### Step 1 — 逆作用素 A†（論文 Eq. 1）

```
A† = Xᵀ Ŷ (Ŷᵀ Ŷ + ε I_m)⁻¹            (ε > 0 は数値安定化のための ridge)
```

- これは **大域的な線形逆サロゲート**であって、ブラックボックスの厳密な逆ではない。
- 「元のモデルが大域線形」と仮定しているのではなく、**逆サロゲートが線形**なだけ（論文 §2 の明示 2）。
- 摂動サンプリングを使わないため、SHAP/LIME と違い**決定論的**で、再実行しても同じ `A†` が出る。

### Step 2 — ルール誘導（論文 §3.2）

`A†`, `X`, `Ŷ` から、クラスごとに「十分重要な特徴」（`|A†_{·,k}|` 上位 `top_k`）に対して 3 種類の単純ルールを**データ駆動で**誘導する。

1. **単調性 `M_{jk}`**：`+1`＝寄与は非負であるべき、`−1`＝非正、`0`＝制約なし。重要特徴に対してのみ符号を付ける。
2. **閾値 `t_{jk}`**：クラス `k` と予測された事例の特徴値から推定。
   - 連続特徴：分位点（既定は中央値）。
   - 2 値特徴：2 値の中点。
   - **疎な特徴（TF–IDF 等）：presence ルール `t = 0`（= 出現の有無）**。
3. **ペアワイズ共起（AND）**：高寄与特徴のペアについて
   ```
   (x_{j1} ≥ t_{j1k}) ∧ (x_{j2} ≥ t_{j2k}) ⇒ k
   ```
   （対応する単調性が負なら向きを反転）。**可解性と可読性のため pairwise までに限定**し、より高次の連言や XOR 的相互作用は対象外（論文 §2 明示 3、§5）。

### Step 3 — ルール誘導付き再制約（論文 Eq. 2）

```
min_B  ‖B − A†‖²_F  +  α·R_mono(B; M)  +  β·R_rule(B; T, P)
```

- `R_mono`：符号違反を抑えるヒンジ罰則。
- `R_rule`：違反した閾値・ペアワイズの前件に対するヒンジ罰則。
- 第 1 項（二次の近接項）＋凸ヒンジ罰則なので**全体は凸**。勾配法で安定に解ける。
- `B` は `A†` から出発する：近接項が `B` を生の逆サロゲートの近くに留め、ヒンジ項が前件を満たす事例でクラススコアを規則整合な側へ押す。

### Step 4 — Dual reporting と監査セーフガード（論文 §3.4）

制約後の説明を**生 AIME のサイレントな置き換えにしない**。常に
(1) 生の `A†`、(2) 制約後の `B`、(3) 両者の大きな乖離フラグ、を**併記**する。

```
RD(B, A†) = ‖B − A†‖_F / ‖A†‖_F                      (Eq. 3) 小さいほど生 AIME に近い
GA(B, A†) = (1/m) Σ_k cos( B_{·,k}, A†_{·,k} )        (Eq. 4) 大きいほど方向一致
RS                                                     ルール満足率（高いほど良い）
```

- **`RD` は距離**で**小さいほど良い**。論文は初期稿で「fidelity」と誤称していたのを **Relative Deviation (RD)** に改名した。`RD` は 1 で頭打ちにならない（1 超＝生行列の Frobenius ノルム以上に離れた）。
- `RD` が大きい / `GA` が低い（負含む）ときは、**監査フラグ**として扱う。規則整合化が逆サロゲートを攻めすぎて、問題挙動を正常化（fairwashing）してしまう恐れがあるため（論文 §3.4、参考 [16]）。

### 擬似コード

```
A† = Xᵀ Ŷ (ŶᵀŶ + εI)⁻¹                       # Eq.1
rules = induce_rules(A†, X, argmax(Ŷ))         # §3.2 （A† は読むだけ・不変）
B = A†.copy()
repeat steps:
    g = 2(B − A†)                              # 近接項
    g += α · subgrad R_mono(B; M)              # 符号ヒンジ
    g += β · subgrad R_rule(B; T, P)           # 閾値・AND ヒンジ
    B = B − lr · g                             # 凸 ⇒ 収束
report(A†, B, RD, GA, RS)                       # §3.4 併記＋乖離フラグ
```

---

## 4. 論文の「違う点 / 新規性」

| 比較対象 | NS-AIME の差分 |
|---|---|
| **LIME / SHAP** | 摂動サンプリングに依存せず、AIME の**決定論的逆作用素**から出発（サンプリング・背景・局所性の選択でブレない）。NS-AIME は加えて**大域的な論理構造**（単調性・閾値・AND）を明示的に課す。 |
| **AIME（基底手法 [3]）** | AIME の `A†` は数学的に well-defined だが論理構造を保証しない。NS-AIME は `A†` の**解釈可能性**を、近接性を保ったまま凸最適化で底上げする。**AIME 本体は一切変更しない**。 |
| **ルール学習器（SBRL/IDS/CORELS/QCBA 等）** | これらは**予測ルール集合**を最適化する＝ブラックボックスをルール予測器で置換する。NS-AIME はブラックボックスを置き換えず、**別モデルが出した説明行列の「形」を最適化**する。 |
| **AMIE [13]** | 名前が似るが別物。AMIE は知識ベースから Horn ルールをマイニング。NS-AIME は教師あり分類器の**特徴属性行列**を post-hoc に再制約。 |
| **従来の「説明の正しさ」指標** | 「fidelity」という誤称をやめ、**距離 = RD（小さいほど良い）**と**方向一致 = GA**に分離。さらに**大きな乖離を隠さず監査シグナルとして提示する dual reporting** を方法論の核に据える。 |

**論文が主張しないこと**（スコープ）：一般の説明忠実性を解決する／専用ルール学習器に優越する、とは主張しない。証拠は 2 データセット（Titanic、20 Newsgroups）× 3 古典モデル（ロジスティック回帰・ランダムフォレスト・LightGBM）に限定。密な画像・系列は対象外（Eq.1 の支配項 `Xᵀ Ŷ` が疎積で効くため、疎入力に実務的）。論文 Table 1 の代表値：20NG で RS が 0.02–0.15 → 0.75–0.93 に大幅改善、Titanic では RS が小幅改善する一方 GA が負になる場合がある（＝監査フラグ）。

---

## 5. 理論と実装の対応（本リポジトリ `ns_aime_xai2026`）

| 論文 | 実装 |
|---|---|
| Eq. 1（逆作用素） | `core.NSAIME._inverse_operator` |
| §3.2（ルール誘導） | `utils.induce_rules` |
| Eq. 2（再制約） | `core.NSAIME.optimize` |
| Eq. 3 RD / Eq. 4 GA / RS | `utils.compute_metrics`（旧 `calculate_metrics` はタプル後方互換） |
| §3.4 dual reporting | `visualization.plot_dual_report`, `interactive.render_logic_graph_html` |
| §4.3 logic graph | `visualization.plot_logic_graph`（静的・キャッチー）, `interactive`（動的） |

### 明示する仮定（論文に式の細部がないため）

1. **標準化**：`normalize=True` のとき、`A†`・スコア `X·B`・閾値（数値計算用）はすべて**標準化空間**で一貫して扱う。一方、グラフに表示する閾値は**元の単位**で出す（`threshold_display`）。論文は標準化の有無を明記していないため、これは本実装の仮定。
2. **`R_rule` / RS の解釈**：論文の「前件が満たされたとき規則が成り立つ」を**スコアレベル**で解釈する。すなわち、前件 `x_j ≥ t` を満たす事例について、クラススコア `x·B_{·,k}` が規則整合な符号（正規則なら `≥ +γ`、負規則なら `≤ −γ`）を持つかで評価する。`R_mono` は**係数レベル**（`sign(B_{jk}) = M_{jk}`）。
3. **マージン `γ`**：満足判定に用いる小さなマージン。RS 指標自体は符号（`γ=0`）で測る。

### 既存実装からの修正点（= 論文準拠への是正）

本タスク前の `ns_aime_xai2026` には論文と**乖離する実装**があったため、ユーザ承認のもと是正した：

1. **`A†` の in-place 書き換えを廃止**。旧 `utils.induce_rules` は "Force A_dagger consistency" として `A_dagger[idx,k]` を上書きしていた。これは §3.4 の dual reporting を壊す（`A†` が生の逆サロゲートでなくなり、RD/GA が無意味化する）。修正後、`A†` は**完全に不変**（`fit`/`optimize` 後も `A†` が `Eq.1` の再計算と一致することをテストで検証）。
2. **`fidelity` → `RD`** に改名（論文 Eq.3 の訂正に追従）。タプル API の戻り値先頭は RD。
3. **逆作用素を Eq.1 の ridge 形に明示**（`Xᵀ Ŷ (ŶᵀŶ + εI)⁻¹`）。旧実装は `Xᵀ (Ŷᵀ)⁺` で `ε→0` 極限では一致するが、`ε` を陽に持つ方が論文に忠実かつ安定。
4. **最適化器の安定化**：減衰ステップ＋ルール数で正規化した平衡勾配に変更。旧実装は固定 lr で発散気味（Titanic で RS≈0.007 と破綻）だったが、修正後は `β` に対して RS が単調増加→飽和し、近接項とのトレードオフ（`B` の崩壊抑制）が保たれる。
5. 依存削減：`sklearn`（標準化）・`networkx`（描画）への必須依存を撤廃（numpy 標準化＋純 matplotlib 描画）。`scipy` は疎入力時のみ任意。再現性・移植性のため。

### 将来拡張との接続（プロジェクト方針）

逆作用素 `A†` を起点に「形」を再制約する枠組みは、Graph-AIME（グラフ構造前件）、Prescriptive AI（前件→介入）、QAIME（不確実性つきルール）へ自然に拡張できる。`R_rule` の前件集合を差し替えるだけで上位の連言・グラフ制約に一般化可能（本実装は pairwise まで）。

---

## 6. 限界（論文準拠）

- 評価は 2 データセット・3 古典モデルのみ。ルール学習/抽出（SBRL, IDS, CORELS, QCBA）との直接比較は未実施。
- ルールは単調性・閾値・pairwise AND まで（高次連言・選言・XOR 的相互作用は範囲外）。
- 密な連続モダリティ（生画像など）には未対応。適用には特徴グルーピング・埋め込みレベル制約等の前処理が要る。
- ユーザを対象とした「監査しやすさ」の検証は未実施。
